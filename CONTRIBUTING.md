# Contributing

Issue・PRを歓迎します。

## Issue

- バグ報告: 再現手順・期待した動作・実際の動作を書いてください
- 機能提案: どんな業務シーンで困っているか(Whyから)書いてもらえると議論しやすいです

## PR

1. Fork してブランチを切る
2. `template/` 配下を変更した場合は、`scripts/install.sh` での配布に影響がないか確認
3. 日本語のドキュメント・コメントで統一(このプロジェクトは日本語話者の非エンジニア業務利用を主対象にしています)
4. コミットメッセージは Conventional Commits (`feat:`, `fix:`, `docs:` など) に従う

## 設計思想を変える提案について

`fde-guide.md`(規範)や `template/AGENTS.md` の安全設計(承認線・自律度・機密の扱い)に関わる変更は、
背景にある「AIに何を任せてよいか」という前提を崩さないよう、Issueで先に議論してからPRをお願いします。
